<!--components/songItem/songItem.wxml-->
<view class="item">
  <image  class=".image" src="{{item.al.picUrl}}"></image>
  <view class="content">
    <view class="name">{{item.name}}</view>
     <view class="source">{{ item.ar[0].name}}</view>
  </view>
  <view class="arrow">
  <van-icon name="arrow" color="#999" size="16px"></van-icon>
  </view>
</view>