<!--pages/detail-search/detail-search.wxml-->
<van-search class="search" value="{{ value }}" input-align="center" use-action-slot placeholder="请输入搜索关键词" bind:change="onChange">
  <view slot="action" bind:tap="onClick" class="btn">搜索</view>
</van-search>

<!-- <view class="Mysearch">
  <view class="header">
    <view class="title"> 搜索历史</view>
    <view class="clear">清空历史</view>
  </view>
  <view class="content"></view>
</view> -->
<view class="Hotsearch">
  <view class="title"> 热门搜索</view>
  <view class="content">
   <block wx:for="{{HotSearch}}" wx:key="{{item}}">
    <van-tag plain size="large" type="primary">{{item.first}}</van-tag>
    </block>
   
  </view>
</view>